import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  input: {
    fontSize: 16,
    borderWidth: 1,
    borderColor: '#ddd',
    paddingVertical: 12,
    paddingHorizontal: 20,
    marginBottom: 15,
  },
  textarea: {
    minHeight: 58,
  },
});
